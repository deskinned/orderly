//! Tests for .skin file validation
import { describe, it } from 'vitest';

describe('validate', () => {
  it.todo('accepts valid .skin file');
  it.todo('rejects missing meta.id');
  it.todo('rejects invalid token paths');
  it.todo('rejects invalid tags');
});
