//! Tests for rate limiting
import { describe, it } from 'vitest';

describe('rate limit', () => {
  it.todo('allows first request');
  it.todo('blocks exceeding per-user hourly limit');
  it.todo('blocks exceeding per-user daily limit');
  it.todo('blocks exceeding global hourly limit');
});
