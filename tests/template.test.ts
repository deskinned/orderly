//! Tests for PR template generation
import { describe, it } from 'vitest';

describe('template', () => {
  it.todo('generates correct PR title');
  it.todo('includes theme metadata in PR body');
  it.todo('lists components and tokens');
});
